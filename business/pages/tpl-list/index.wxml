


<view class="act-view">

    <van-radio-group value="{{ radio }}" bind:change="onChange">

  <van-radio  name="{{item.freight_tpl_id}}"  wx:for="{{lists}}">{{item.freight_tpl_name}}<block wx:for="{{item.freight_tpl_info}}" wx:for-key="city">
  	{{city.name}}
  </block></van-radio>

</van-radio-group>

    <view class="tpl-add">
        <button class="linenear_bg"  bindtap="goSetPrice"> 新增运费模板 </button>
    </view>

</view>
























