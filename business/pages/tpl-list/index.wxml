


<view class="act-view">

    <van-radio-group value="{{ radio }}" bind:change="onChange">

  <van-radio disabled name="{{item.freight_tpl_id}}"  wx:for="{{lists}}">{{item.freight_tpl_name}}
  	<button size="mini" bindtap="edit" data-id="{{item.freight_tpl_id}}">修改</button>
  		<button size="mini" class="aa"  bindtap="remove" data-id="{{item.freight_tpl_id}}">删除</button>

  	<block wx:for="{{item.freight_tpl_info}}" wx:for-key="city">
  	{{city.name}} 
  </block></van-radio>

</van-radio-group>

    <view class="tpl-add">
        <button class="linenear_bg"  bindtap="goSetPrice"> 新增运费模板 </button>
    </view>

</view>

<van-dialog id="van-dialog" />

























