<view class="container gray-wrap cf" style="padding-top: 100rpx;">
    <block wx:if="{{address.length>0}}">
        <view class="fixed-btn">
            <navigator url="../address-form/index" class="btn xxl primary block">
                添加新地址
            </navigator>
        </view>
        <view class="content-wrap">
            <view class="white-bg mv-sm">
                <van-swipe-cell right-width="{{ 60 }}" wx:for="{{address}}" wx:key="{{index}}" disable>
                    <view class="address-item">
                        <view class="flex align-center">
                            <view class="mr-df ttm-checkbox align-self-center {{item.address_id==selected?'checked':''}}" wx:if="{{source}}" data-id="{{item.address_id}}" bindtap="setSelected">
                                <van-icon name="success" custom-class="iconsuccess" />
                            </view>
                            <view class="bd" data-id="{{item.address_id}}" bindtap="setSelected">
                                <view class="text-lg bold"><text class="mr-sm">{{item.consignee}}</text><text class="mr-sm">{{item.mobile}}</text>
                                    <text wx:if="{{item.is_address_default==1}}" class="btn lt primary">默认</text>
                                </view>
                                <view class="text-gray mt-xs" >
                                   {{item.citynames[4]}}{{item.address}}
                                </view>
                            </view>
                            <navigator url="../address-form/index?id={{item.address_id}}" hover-class="none" class="fd solid-left">
                                <van-icon name="records" size="44rpx"/>
                            </navigator>
                        </view>
                    </view>
                    <view slot="right" class="address-del coral" bindtap="remove" data-id="{{item.address_id}}" data-add="{{item}}">删除</view>
                </van-swipe-cell>
            </view>
        </view>
    </block>
    <view class="no-content white-bg" style="height:100vh" wx:else>
        <view class="no-content-bd">
            <image src="http://leileiimg.countinsight.com/front/img/address_none.png" class="no-content-icon"></image>
            <view class="no-content-des"><text>暂无收货地址</text></view>
            <view class="no-content-btn">
                <navigator url="../address-form/index" class="btn primary lg round w-min-xl">添加</navigator>
            </view>
        </view>
    </view>
</view>