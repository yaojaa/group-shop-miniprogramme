<view class="container">
    <view class="collect-tips" bindtap="closleTips" style="height:{{CustomBar}}px;padding-top:{{StatusBar}}px;" wx:if="{{show_tips}}">
        <view class="collect-box">"点击右边
            <van-icon name="weapp-nav" size="28rpx;" /> 添加到我的小程序"
            <van-icon name="close" size="28rpx;" />
        </view>
    </view>

    <view class="head-box">

<!--             <view class="top_user mini-tag mt-xs">供应商入驻</view>
 -->
        <view class="flex align-center">
            <navigator url="../identify/identify" hover-class="none">
                <view class="user-avatar mh-df">
                    <image class="user-avatar" src="{{userInfo.headimg}}" />
                    <view class="set-icon">
                        <i-icon type="setup_fill" color="#ffa70f" size="16" />
                    </view>
                </view>
            </navigator>
            <view class="flex-sub pr-lg">
                <view class="flex justify-between text-white">
                    <view class="user-box">
                        <view class="user-name mr-sm">{{userInfo.nickname}} </view>
                        <text   bindtap="goSite" class="f mini-tag mt-xs" hover-class="none" hover-class="none" >我的专属主页
                        </text>
                       
                    </view>
                <!--     <navigator url="../fans-mg/index" hover-class="none" class="text-center" wx:if="{{!isCustome}}">
                        <view class="text-lg bold">{{fansNum}}</view>
                        <view class="text-sm mt-xs">我的粉丝</view>
                    </navigator> -->
                    <navigator url="../withdrawal/index" hover-class="none" class="text-center">
                        <view class="text-lg bold">¥ {{pending_money}}</view>
                        <view class="text-sm mt-xs">账户提现</view>
                    </navigator>
                </view>
            </view>
        </view>
    </view>

   <view class="new_order"  wx:if="{{orderList.length}}"> <button class="listen_btn hollow" bindtap="addListen">+订阅提醒</button>
    <i-panel hide-border title="最新订单" extra="更多" link="../new-order-list/index">
        <view class="control">

            <view class="newlist">

                <scroll-view style="height: 100px" scroll-y>
                    <view class="item" wx:key="index" wx:for="{{orderList}}" data-id="{{item.order_detail[0].goods_id}}" data-delivery_method="{{item.delivery_method}}" data-name="{{item.order_detail[0].goods_name}}" bindtap="managePage">
                        <text class="bold">{{item.user.nickname}}</text> {{item.time_ago}}
                        参与了你的
                        <text class="bold">{{item.order_detail[0].goods_name}}</text>
                    </view>
                </scroll-view>
            </view>
        </view>
    </i-panel>
    </view> 


<!--     <i-panel i-class="marginTop" title="我参与的" extra="查看全部订单" link="../order-list/index">
        <view slot="content">
            <view class="flex-box myorder">
                <navigator url="../order-list/index?search_status=1" hover-class="none" class="flex-item">
                    <view class="icon">
                        <i-icon type="redpacket" size="28" color="#4fac05" />
                    </view>
                    待付款
                    <view class="badge" wx:if="{{waitpay>0}}">{{waitpay}}</view>
                </navigator>
                <navigator url="../order-list/index?search_status=2" hover-class="none" class="flex-item">
                    <view class="icon">
                        <i-icon type="service" size="28" color="#4fac05" />
                    </view>
                    待收货<view class="badge" wx:if="{{waitreceived>0}}">{{waitreceived}}</view>
                </navigator>
                <navigator url="../order-list/index?search_status=3" hover-class="none" class="flex-item">
                    <view class="icon">
                        <i-icon type="interactive" size="28" color="#4fac05" />
                    </view>
                    已完成
                </navigator>
                      <view class="flex-item">
             <view class="icon">
                    <i-icon type="customerservice" size="28" color="#4fac05" />
                </view>
            退款售后</view>
            </view>
        </view>
    </i-panel> -->




    <view class="text-center">
        <wux-spin wx:if="{{is_loading}}"></wux-spin>
    </view>
    <view class="panelbox">

      
        <!--   <button class='new-btn hollow' bindtap="new_btn">
          <i-icon type="add" size="20" />新发布
        </button> -->

        <i-panel  wx:if="{{goodslist.length > 0 }}" >
<!--             <van-search value="{{ value }}" placeholder="输入标题关键字搜索" background="#ffffff" 
            show-action
  bind:search="onSearch"
  /> -->

            <view class="goods-box">
                <block wx:key="index" wx:for="{{goodslist}}">
                    <i-goods-item bindrecommend="recommendHandle" bindremove="removeHandle" item="{{item}}" index="{{index}}" />
                </block>
                <view class="text-center">
                    <wux-spin wx:if="{{is_loading}}"></wux-spin>
                </view>
            </view>
        </i-panel>
    </view>
    <i-panel wx:if="{{goodslist.length== 0 && !is_loading }}">
        <view class='new-goods' bindtap="new_btn" style="margin-bottom:100rpx">
            <text>+ 立即发布活动 </text>
        </view>

        <button style="margin:0 auto; display: block;" size="mini" open-type="contact" bindcontact="handleContact"> <van-icon name="chat-o" /> 使用咨询</button>

    </i-panel>
    <view style="margin-bottom:100rpx"></view>
    <!--  <view wx:if="{{goodslist.length >= 0}}">
        <view class='new-goods' bindtap="new_btn">
            <i-icon type="add" size="22" />
            <text>发布一个团购 </text>
        </view>
    </view> -->
    <!--    <view class="footer_bar">
        <i-tab-bar current="nearby" color="#188f00" bindchange="handleTabBarChange">
            <i-tab-bar-item key="nearby" icon="browse" current-icon="browse_fill" title="我的"></i-tab-bar-item>
            <i-tab-bar-item key="publish" icon="add" current-icon="add" title="发布"></i-tab-bar-item>
            <i-tab-bar-item key="home" icon="mine" current-icon="mine_fill" title="主页"></i-tab-bar-item>
        </i-tab-bar>
    </view> -->
    <van-dialog id="van-dialog" />
  
    <footerbar/>


</view>