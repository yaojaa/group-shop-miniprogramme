<i-custom-bar backColor="#fff" showIcon="{{true}}" title="添加提货地点"> </i-custom-bar>


<view class="container mtop100">

<view class="bgimg">

  

<block wx:for="{{newAddress}}" wx:key="id">
  <view class='address button-area'>
  <!-- <view class='address' wx:if="{{index == 0}}"> -->
    <view class='icon-img'>
      <i-icon type="coordinates" size="48" color="#ddd" />
    </view>
    <view class='content'>
      <view class='name'>{{item.name}}</view>
      <view class='des'>{{item.address}}</view>
      <view class='des door_number'>
        <text class="text-danger">门牌号：</text>
        <input value="{{item.door_number}}" bindinput='inputAddressDes' focus='ture' placeholder="如：2号楼3单元203" type="text" name=""/>
      </view>

<!--       <view class='limit'>
    <view class='header'>
      <text>限制参与</text>
      <switch bindchange="limitChange"/>
    </view>
    <view wx:if="{{ hidden }}" class='control'>
      <view>周边半径范围（km）</view>
      <view>
        <i-input-number value="{{ limitVal }}" min="0" max="100" step="{{step}}" bindchange="handleChange" />
      </view>
    </view>

  </view>
 -->
    </view>
    <view class='delete' bindtap='deleteAddress' data-type="new" data-id="{{item.id}}"><icon type="clear" size="28"/></view>

    


  </view>



  <!-- <view class='address' wx:else>
    <view class='icon-img'>
      <image mode="aspectFit" data-id="{{item.id}}" src="{{location}}"></image>
    </view>
    <view class='content' data-id="{{item.id}}" bindtap='selectAddress'>
      <view class='name'>{{item.name}}</view>
      <view class='des'>{{item.address}}</view>
      <view wx:if="{{item.door_number}}">详细门牌号：{{item.door_number}}</view>

    </view>
    <view class='delete' bindtap='deleteAddress' data-type="new" data-id="{{item.id}}"><icon type="clear" size="18"/></view>
  </view> -->

</block>


<view class="center" style="margin-top:60rpx">

<view  wx:if="{{openLocation}}" bindtap='addAddress'>
   <button  class="btn btn-round linenear_bg" >
      <i-icon type="add" size="20" color="#fff" /> 
  地图标注 
      </button>

</view>

  <view class='get-location' wx:else>
    <button  open-type='openSetting' bindopensetting="openSet"><text class='icon'>+</text><text>去授权添加地点</text></button>
  </view>

</view>



</view>


<!-- <block wx:if="{{oldAddress.length > 0}}">
<view class='history'>历史记录（{{oldAddress.length}}），点击下面记录添加活动地点</view>
<view class='session'>
<block wx:for="{{oldAddress}}" wx:key="id">
  <view class='address'>
    <view class='icon-img'>
      <i-icon type="coordinates" />
    </view>
    <view class='content' data-id="{{item.id}}" bindtap='moveAddress'>
      <view class='name'>{{item.name}}</view>
      <view class='des'>{{item.address}}</view>
      <view class='des' wx:if="{{item.door_number}}">提货点：{{item.door_number}}</view>
    </view>
    <view class='delete' bindtap='deleteAddress' data-type="old" data-id="{{item.id}}"><icon type="clear" size="18"/></view>
  </view>
  <view class='line'></view>
</block>
</view>
</block> -->


<!-- <i-button  i-class='button linenear_bg' bind:click="submit" type="success" long="true">选好了</i-button> -->


   <button wx:if="{{newAddress.length}}" bindtap="addPicture" class="btn btn-round linenear_bg_light" >
        完成返回
   </button>




</view>